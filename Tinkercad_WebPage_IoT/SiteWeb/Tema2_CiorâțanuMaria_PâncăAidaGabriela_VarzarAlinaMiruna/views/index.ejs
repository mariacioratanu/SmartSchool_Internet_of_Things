<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Gas Sensor Data</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container">
    <header>
      <h1>Gas Sensor Data Management</h1>
    </header>
    <div class="content">
      <div class="upload-form">
        <h2>Upload Sensor Data</h2>
        <form action="/upload" method="post" enctype="multipart/form-data">
          <div class="form-group">
            <label for="file">Select Gas Sensor Data File (.txt)</label>
            <input type="file" id="file" name="file" accept=".txt" required>
          </div>
          <button type="submit">Upload and Process</button>
        </form>
        <% if (message) { %>
          <div class="message <%= messageType %>">
            <%= message %>
          </div>
        <% } %>
      </div>
      
      <h2>Gas Sensor Readings</h2>
      <% if (readings && readings.length > 0) { %>
        <table>
          <thead>
            <tr>
              <th>ID</th>
              <th>Gas Value</th>
              <th>Timestamp</th>
            </tr>
          </thead>
          <tbody>
            <% readings.forEach(reading => { %>
              <tr>
                <td><%= reading.id %></td>
                <td><%= reading.gas_value %></td>
                <td><%= new Date(reading.timestamp).toLocaleString() %></td>
              </tr>
            <% }); %>
          </tbody>
        </table>
      <% } else { %>
        <div class="no-data">No gas sensor readings found in the database</div>
      <% } %>
      
      
    </div>
  </div>
</body>
</html>